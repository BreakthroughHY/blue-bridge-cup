#pragma once

#ifndef __TRIANGLE_MINIMUM_PATH_SUM_H__
#define __TRIANGLE_MINIMUM_PATH_SUM_H__

#include "main.h"

// ÀàÉùÃ÷
class SolutionTriangleMinimumPathSum {
public:
    int minimumTotal(vector<vector<int>>& triangle);

    int minimumTotalNew(vector<vector<int>>& triangle);
};

int triangleMinimumPath();

#endif // !__TRIANGLE_MINIMUM_PATH_SUM_H__
